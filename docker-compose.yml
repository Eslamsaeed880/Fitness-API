services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - .:/build
      - /build/node_modules
    command: npm run dev
